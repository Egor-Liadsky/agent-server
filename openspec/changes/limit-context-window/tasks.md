## 1. Конфигурация

- [x] 1.1 Добавить `AGENTD_MAX_CONTEXT_TOKENS` в `AgentdConfig` и
      `from_source` (`src/config.rs`): необязательное положительное целое,
      фатальная ошибка старта при некорректном значении; проверить тестом
      конфигурации на отсутствующее, корректное и некорректное значение
- [x] 1.2 Задокументировать переменную в README рядом с остальными
      `AGENTD_*`

## 2. Контракт `/v1/chat`

- [x] 2.1 Добавить `max_context_tokens: Option<u32>` в `ChatSettingsDto`
      (`src/dto.rs`) и убедиться, что поле не попадает в сохраняемые
      настройки чата (только на один запрос)
- [x] 2.2 Добавить коды ошибок `context_limit_invalid` и
      `context_limit_exceeded` в `src/error.rs` по формату единого
      конверта ошибок
- [x] 2.3 Задокументировать поле и коды ошибок в README рядом с описанием
      `POST /v1/chat`

## 3. Проверка лимита

- [x] 3.1 Реализовать оценку размера истории в токенах (эвристика по
      длине текста, `ceil(символы / 4)`) как отдельную функцию в
      `src/app.rs` и покрыть её модульным тестом на границах округления
- [x] 3.2 Реализовать вычисление эффективного лимита (клиентский
      `max_context_tokens`, операторский `AGENTD_MAX_CONTEXT_TOKENS`, или
      отсутствие проверки) и отказ `400 context_limit_invalid` при попытке
      клиента увеличить лимит выше операторского умолчания
- [x] 3.3 Встроить проверку эффективного лимита перед сборкой
      `RequestContext` в обоих обработчиках `/v1/chat` (с `chat_id` и без);
      при превышении — `400 context_limit_exceeded` без вызова провайдера
      и без записи в историю чата

## 4. Тесты

- [x] 4.1 Тест: запрос без `AGENTD_MAX_CONTEXT_TOKENS` и без
      `settings.max_context_tokens` не проверяет размер и доходит до
      провайдера (через `wiremock`)
- [x] 4.2 Тест: клиентское значение меньше операторского лимита принимается
      как эффективный лимит
- [x] 4.3 Тест: клиентское значение больше операторского лимита (и случай
      без операторского лимита) отклоняется `400 context_limit_invalid`
- [x] 4.4 Тест: история, превышающая эффективный лимит, отклоняется
      `400 context_limit_exceeded` без обращения к `wiremock`-заглушке
      провайдера
- [x] 4.5 Прогнать `cargo test` целиком и убедиться, что существующие
      тесты `/v1/chat` не сломаны
