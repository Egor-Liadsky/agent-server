## 1. Вход автотрекера (design.md, решение 1)

- [x] 1.1 Добавить параметр с текстом последнего сообщения пользователя
      в `track_after_exchange` (`agent-sever/src/task.rs`) и передать в
      него `prompt` из фонового запуска в `handle_chat_in_existing`
      (`src/app.rs`); проверка — `cargo build` без предупреждений
- [x] 1.2 Переписать `tracker_prompt`: два отдельных блока («Последнее
      сообщение пользователя», «Последний ответ модели») и критерии
      переходов в их терминах — `clarification` по вопросам в ответе
      модели, `execution` по подтверждению в сообщении пользователя
      (specs/task-state, «Трекер учитывает этап уточнения»)
- [x] 1.3 Добавить тест в `src/tests.rs`, который мокает провайдера по
      `TASK_TRACKER_MARKER` и проверяет, что тело запроса трекера
      содержит и текст реплики пользователя, и текст ответа модели
      (сценарий «Вход трекера содержит обе реплики обмена»)

## 2. Предложение с текущим этапом (design.md, решение 2)

- [x] 2.1 Добавить в `track_after_exchange` ветку `to == from`:
      проверка лимитов длины, применение через
      `store::update_task_fields`, без записи в журнал переходов
- [x] 2.2 Расширить `TrackOutcome` счётчиком обновлений без смены этапа
      и перестать учитывать такой исход как `rejected`
- [x] 2.3 Добавить поле счётчика в `ContextDto` (`src/dto.rs`) и
      перенос значения в `merge_tracker_counters`
      (`src/task.rs`, `src/context.rs`); проверка — существующие тесты
      блока `context` собираются и проходят
- [x] 2.4 Добавить тест в `src/tests.rs`: предложение трекера с текущим
      этапом и новым шагом обновляет шаг, этап не меняется, журнал
      переходов не растёт, счётчик обновлений равен 1, отклонений — 0
      (сценарии «Предложение текущего этапа обновляет шаг и ожидаемое
      действие», «Ответ различает обновление полей и переход»)

## 3. Ручной переход в `done` (design.md, решение 3)

- [x] 3.1 Вынести в `src/task.rs` проверку завершения задачи: этап
      `validation` и отсутствие паузы, с отказами `InvalidEdge` и
      `Paused`; вызвать её из ветки `Some("done")` в
      `handle_task_transition` (`src/app.rs`) до `store::finish_task`
- [x] 3.2 Добавить тесты в `src/tests.rs` на `400` для `{"stage":"done"}`
      с `planning`/`clarification`/`execution` и для задачи на паузе, с
      проверкой, что `active_task_id` чата и рабочая память не
      изменились (сценарии «Переход в done вне validation отклоняется»,
      «Переход в done на паузе отклоняется»)
- [x] 3.3 Проверить тестом, что `{"stage":"done"}` с `validation`
      по-прежнему завершает задачу и возвращает `new_task_id`, а
      `POST /v1/chats/{id}/memory/working/finish-task` не затронут

## 4. Неизвестный этап (design.md, решение 4)

- [x] 4.1 Вернуть `TaskError::UnknownStage` из `apply_manual_transition`
      для имени этапа вне закрытого набора и добавить тест на текст
      «неизвестный этап задачи» в ответе `400`, не задевая текст отказа
      для известного недопустимого перехода (сценарии из ADDED
      Requirements)

## 5. Клиент `agent-cli` (design.md, решение 5)

- [x] 5.1 Привести `TASK_STAGE_EDGES` в
      `agent-cli/crates/client/src/chats.rs` к семи серверным рёбрам,
      включая `clarification`
- [x] 5.2 Добавить тест в `crates/client/src/tests.rs` на
      `allowed_next_stages` для всех пяти этапов (из `planning` —
      `clarification`; из `clarification` — `execution` и `planning`;
      из `done` — пусто)
- [x] 5.3 Обновить тесты экрана состояния задачи в
      `crates/cli/src/tui.rs`, если они опираются на старый список
      переходов; проверка — `cargo test` в `agent-cli` зелёный
- [x] 5.4 Обновить `agent-cli/README.md` в описании `Ctrl+T`: этапы
      перечисляются с `clarification`

## 6. Сценарий ручного прогона (design.md, решение 6)

- [x] 6.1 Создать `agent-sever/docs/manual-test-task-state.md` из
      приложения `design.md` (подготовка окружения, сценарии А, Б, В,
      признаки неисправности)
- [x] 6.2 Сослаться на этот документ из раздела состояния задачи в
      `agent-sever/README.md`
- [x] 6.3 Прогнать сценарии А и Б вручную против живого провайдера с
      ключом из `agent-sever/.env` и убедиться, что этап меняется на том
      же обмене, где появился сигнал, а шаг и ожидаемое действие
      обновляются каждый обмен

## 7. Документация и стабильность прогона

- [x] 7.1 Обновить раздел состояния задачи в `agent-sever/README.md`:
      критерии трекера по двум репликам, ограничение `done` ребром
      `validation → done` и паузой, новый счётчик в блоке `context`,
      отдельная причина отказа для неизвестного этапа
- [x] 7.2 Изолировать `state::tests::agent_is_created_once_for_many_requests`
      от параллельных тестов, создающих `AppState` (design.md,
      решение 7); проверка — три подряд полных прогона `cargo test`
      зелёные
- [ ] 7.3 Прогнать `cargo test` в `agent-sever` и `cargo test` в
      `agent-cli`, убедиться, что оба зелёные, и закоммитить изменения
      в каждом подмодуле отдельно, зафиксировав новые указатели в
      зонтичном репозитории
