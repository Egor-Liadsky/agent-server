## 1. Зависимости и конфигурация

- [x] 1.1 Добавить в `Cargo.toml` зависимости `sqlx` (`features = ["runtime-tokio", "sqlite", "migrate"]`), `sha2` и dev-зависимость `tempfile`; проверить `cargo build` и убедиться через `grep -A2 'name = "agentcore"' Cargo.lock`, что подмена ядра осталась прежней
- [x] 1.2 Добавить в `AgentdConfig` поля `db_path`, `db_max_connections`, `db_busy_timeout_ms` с чтением `AGENTD_DB_PATH` (по умолчанию `agentd.db`), `AGENTD_DB_MAX_CONNECTIONS` (по умолчанию `5`) и `AGENTD_DB_BUSY_TIMEOUT_MS` (по умолчанию `5000`); проверить тестами в `src/config.rs`, что умолчания применяются, а нечисловое и нулевое значение дают ошибку
- [x] 1.3 Добавить `agentd.db`, `agentd.db-wal`, `agentd.db-shm` в `.gitignore` и убедиться `git status`, что файл базы после локального запуска не попадает в изменения

## 2. Схема и миграции

- [x] 2.1 Создать `migrations/0001_init.sql` с таблицами `chats` и `messages`, внешним ключом `ON DELETE CASCADE`, ограничением `UNIQUE (chat_id, seq)` и индексом `chats_owner_updated` по описанию из `design.md` (решение 3)
- [x] 2.2 Реализовать открытие пула с `journal_mode = WAL`, `foreign_keys = ON`, `busy_timeout` и `synchronous = NORMAL`, созданием каталога базы и применением `sqlx::migrate!()`; проверить тестом, что на пустом каталоге база создаётся и схема применяется
- [x] 2.3 Проверить тестом идемпотентность: повторное открытие той же базы проходит без ошибки и не меняет ранее записанные строки
- [x] 2.4 Проверить тестом, что каскадное удаление работает (то есть `foreign_keys = ON` выставлен на соединении пула, а не только в миграции)

## 3. Слой хранилища

- [x] 3.1 Создать модуль `src/store.rs` с типами `Chat`, `ChatMessage`, `NewMessage` и типизированной ошибкой хранилища, независимыми от axum и `ApiError`; проверить `cargo build`
- [x] 3.2 Реализовать `create_chat`, `load_chat`, `update_chat`, `delete_chat` с владельцем в условии выборки; проверить тестами создание, чтение, переименование, изменение настроек и удаление
- [x] 3.3 Реализовать `list_chats` с порядком `(updated_at DESC, id DESC)`, ограничением по владельцу и keyset-курсором; проверить тестом обход трёх страниц без пропусков и повторов, в том числе при одинаковом `updated_at`
- [x] 3.4 Реализовать `load_messages` с параметрами `after` и `limit` и порядком по возрастанию `seq`; проверить тестом дочитывание чата по частям
- [x] 3.5 Реализовать `append_exchange` — одна транзакция `BEGIN IMMEDIATE`, номера `seq` считаются внутри неё; проверить тестом, что два сообщения получают номера `N+1` и `N+2`, а телеметрия и `reasoning` сохраняются и читаются обратно
- [x] 3.6 Проверить тестом конкурентную запись: два одновременных `append_exchange` в один чат дают четыре сообщения с последовательными номерами без потерь
- [x] 3.7 Реализовать отпечаток владельца `sha256("agentd-client-token:v1:" || токен)` и литерал `anonymous` при выключенной аутентификации; проверить тестом, что отпечаток стабилен, отличается для разных токенов и не совпадает с `anonymous`

## 4. Старт сервиса и готовность

- [x] 4.1 Добавить пул в `AppState` и открывать базу в `main.rs` до прослушивания порта; проверить `cargo run`, что сервис поднимается и создаёт файл базы
- [x] 4.2 Сделать непригодную базу фатальной ошибкой старта с путём в сообщении; проверить тестом на недоступном на запись пути, что подготовка хранилища возвращает ошибку с этим путём
- [x] 4.3 Добавить проверку хранилища в `GET /readyz`, не трогая `GET /healthz`; проверить тестами, что при исправной базе `readyz` отвечает `200`, а `healthz` не обращается к базе

## 5. Эндпоинты управления чатами

- [x] 5.1 Добавить в `src/dto.rs` тела и представления чатов (`ChatDto`, `MessageDto` с `usage`/`timing`/`model`, тела создания и изменения, ответы списка и чтения); проверить `cargo build`
- [x] 5.2 Добавить коды `chat_not_found` (404) и `storage_error` (500) в `src/error.rs` и отображение ошибок хранилища в них; проверить тестом, что текст ошибки хранилища клиенту не уходит, а `request_id` в конверте есть
- [x] 5.3 Реализовать `POST /v1/chats` с проверкой заголовка и настроек; проверить тестами создание с умолчаниями, отказ на заголовок из пробелов и на модель вне `AGENTD_ALLOWED_MODELS`
- [x] 5.4 Реализовать `GET /v1/chats` с `limit` и `cursor`; проверить тестами обход страниц, отказ `400` на `limit` сверх предела и на неизвестный курсор
- [x] 5.5 Реализовать `GET /v1/chats/{id}` с полями чата, страницей сообщений и `next_after`; проверить тестами чтение пустого чата и чтение переписки по частям
- [x] 5.6 Реализовать `PATCH /v1/chats/{id}` для заголовка и постоянных настроек; проверить тестами переименование, сохранение настроек, отказ `400` на пустое тело и на недопустимые настройки
- [x] 5.7 Реализовать `DELETE /v1/chats/{id}`; проверить тестом, что первый вызов даёт `204` без тела, повторный — `404`, а сообщения чата больше не читаются

## 6. Диалог в чате

- [x] 6.1 Добавить в `ChatRequest` поле `chat_id` и разбор его сочетаний с `prompt`, `messages` и `settings`; проверить тестами отказ `400` на `messages` вместе с `chat_id` и на отсутствие `prompt` при заданном `chat_id`
- [x] 6.2 Реализовать ветку `POST /v1/chat` с `chat_id`: история из базы, наложение `settings` на один запрос, вызов конвейера, запись обмена одной транзакцией, обновление `updated_at`; проверить тестом с `wiremock`, что модель получает сохранённую историю плюс новое сообщение, а в ответе есть `chat_id` и `seq`
- [x] 6.3 Проверить тестом, что ошибка провайдера не меняет чат: сообщений и `updated_at` после `502` столько же, сколько до запроса
- [x] 6.4 Проверить тестом, что `settings` в `POST /v1/chat` не меняет сохранённые настройки чата, а `PATCH /v1/chats/{id}` меняет
- [x] 6.5 Проверить тестом, что модель чата вне `AGENTD_ALLOWED_MODELS` даёт `400` и чат не изменяется
- [x] 6.6 Проверить тестом, что запрос без `chat_id` работает как прежде: ответ прежней формы с `chat_id` равным `null`, список чатов не пополняется

## 7. Владение и изоляция

- [x] 7.1 Прокинуть владельца из слоя аутентификации в обработчики чатов; проверить тестом, что при непустом `AGENTD_CLIENT_TOKENS` чат виден только создавшему токену
- [x] 7.2 Проверить тестами, что чтение, изменение, удаление и запись обмена в чужой чат отвечают `404 chat_not_found` с тем же телом, что и несуществующий идентификатор, и что модель при этом не вызывается
- [x] 7.3 Проверить тестом, что идентификатор неверного вида даёт `404`, а не `400`
- [x] 7.4 Проверить тестами поведение при выключенной аутентификации: анонимные клиенты делят один список чатов, а после включения токенов анонимные чаты в списке не появляются

## 8. Контейнер и документация

- [x] 8.1 Добавить в `Dockerfile` `COPY migrations ./migrations`, создание каталога `/var/lib/agentd` с владельцем `agentd`, `VOLUME /var/lib/agentd` и `ENV AGENTD_DB_PATH=/var/lib/agentd/agentd.db`; проверить `docker build -t agentd .` и запуском контейнера с томом, что база создаётся в томе и переживает пересоздание контейнера
- [x] 8.2 Обновить README: переменные `AGENTD_DB_*`, таблица эндпоинтов с чатами, поле `chat_id` в `POST /v1/chat`, коды `404`/`500`, требование к случайности `AGENTD_CLIENT_TOKENS`, приватность базы и хранение файла базы в томе; проверить, что все описанные переменные и эндпоинты существуют в коде
- [x] 8.3 Прогнать `cargo test` целиком и убедиться, что тесты хранилища идут на временных базах и не оставляют файлов в рабочем каталоге
